= link_to_edit @post

%h1
  = @post.title
  %small
    = t('activerecord.attributes.post.user') + ':'
    = @post.user.name + ','
    = @post.published_at
%h2= t('headers.tags')
= @post.tag_list
= @post.content.html_safe

= render 'comments/comments'
